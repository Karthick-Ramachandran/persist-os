# Change Requests: Opinionated Presets

No accepted change requests.

Batch scope is fixed to four stacks: `kotlin-android` (new), `python-fastapi` (new), `ios-swift`
(enrich), and `nextjs` (enrich).

Additional stacks (Java/Spring Boot, React Native, Node/NestJS, and others) are deferred to
follow-up features that reuse the content standard defined here.
